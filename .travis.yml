language: go
go:
  - 1.11.x
env:
  - GO111MODULE=on
install: true
script:
  - curl -sfL https://install.goreleaser.com/github.com/golangci/golangci-lint.sh | bash -s -- -b $GOPATH/bin v1.10.2
  - golangci-lint run --enable=goimports,goconst,gocyclo
  - ./codecov.sh
